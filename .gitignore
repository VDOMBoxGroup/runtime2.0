*.pyc
*.pyd
*.so
sources/vscript/tables/lexer.py
sources/vscript/tables/parser.py
applications/*
cache/*
data/*
resources/*
temp/*
# types/ is versioned: a type is server code, and the TinyMCE type now
# carries the document font the LimeOS renderer has to match. Ignoring it
# meant that setting lived on one disk, in two copies, under no history.
fonts/*
sources/*.xml
.DS_Store
.idea/
.vscode
claude-run.bat

# PLY writes these next to the grammar when the lexer or parser changes.
parser.out
parsetab.py
